# Vite LIFF skeleton (optional)

這個資料夾目前先放骨架，等你決定要不要用 Vite/React/Vue 再補齊。

如果你希望我直接做成可跑的專案，我會加上：
- `package.json`（vite + typescript）
- `.env.example`（VITE_LIFF_ID=...）
- `src/main.ts` + `index.html`
- 開發指令：`npm i` / `npm run dev`
